# OpenClaw Skills (Aura hard-fork)

Each subdirectory is a skill. Aura Desktop loads every `<skill>/SKILL.md` it
finds here and exposes the metadata on the Skills page. The bundled set below
is intentionally small — it covers the categories the automation pipeline
looks up most often and keeps the installer lean. Drop additional skills in
new subdirectories to extend Aura.
